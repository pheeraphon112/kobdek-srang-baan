import { PDPAConsent } from "@/components/onboarding/pdpa-consent"

export const metadata = {
  title: "นโยบายความเป็นส่วนตัว | JobMatching",
}

export default function PDPAPage() {
  return <PDPAConsent />
}
